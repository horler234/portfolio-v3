import { keyframes } from "styled-components";

export const ShowNavbar = keyframes`
    0% {
        border-radius: 0;
    }

    50% {
        border-radius: 50%;
    }

    100% {
        border-radius: 0;
    }
`;